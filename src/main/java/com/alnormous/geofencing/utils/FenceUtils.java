package com.alnormous.geofencing.utils;

import com.alnormous.geofencing.entities.Coordinate;
import com.alnormous.geofencing.entities.Fence;

public class FenceUtils {
	
	/**
	 * Look at each pair of coords in fence, see if the line intersects a line from coord.  Count number of intersections.  If odd, then in it's inside.
	 * @param coord
	 * @param fence
	 * @return
	 */
	public boolean isPointInFence(Coordinate coord, Fence fence) {
		return false;
	}
}
